// Array to store the transportation options
let transportation: string[] = ['Honda Motorcycle', 'Audi', 'Ferrari','Honda City'];

// Loop through the array and print the desired message for each item
transportation.map((items) => 
    // Print the message with the name of the transportation option
    console.log(`I would like to own a ${items}`)); // Log the message to the console

